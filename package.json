{
  "name": "calculus-1-workbook",
  "version": "1.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/extract-questions.mjs --write && node scripts/build-browser.mjs",
    "build:browser": "node scripts/build-browser.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
